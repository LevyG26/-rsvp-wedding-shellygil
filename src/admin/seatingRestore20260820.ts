// No longer used - was one-time recovery data for the 2026-08-22 seating
// incident, referenced by a temporary "restore from backup" button that has
// since been removed (see SeatingSection.tsx / AdminDashboard.tsx). Safe to
// delete this file entirely; kept as an empty stub only because it couldn't
// be removed from disk directly.
export {};
